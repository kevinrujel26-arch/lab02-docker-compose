version: "3"

services:

  api01:
    container_name: api01
    build: ./api
    environment:
      - MESSAGE="Hola soy ${MI_NOMBRE}"
    ports:
      - "3000:3000"


  api02:
    container_name: api02
    build: ./api
    environment:
      - MESSAGE="Hola soy ${MI_NOMBRE}"
    ports:
      - "3001:3000"
    
  api03:
    container_name: api03
    build: ./api
    environment:
      - MESSAGE="Hola soy ${MI_NOMBRE}"
    ports:
      - "3002:3000"